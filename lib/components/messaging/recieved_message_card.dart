import 'package:flutter/material.dart';

class RecievedMessageCard extends StatelessWidget {
  final _cardColor = Colors.grey;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
